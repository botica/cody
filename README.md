<div align="center">
  <img src="./imgs/banks.png" alt="strap in" width="200px">
</div>
<p>cody is a command-line agent using openrouter</p>
<p>enter '!' instead of y/n to auto-confirm tool calls for the turn.</p>
<p><code>/clear</code> clears the current conversation context (and resets token/cost counters).</p>
<p>enter <code>---</code> on its own line to begin multi-line input; end your multiple-lines-pasting with <code>---</code> on its own line.</p>
<p>ctrl-c kills program.</p>
<p>--> python agent.py after install reqs and rg</p>
<p><a href="https://github.com/BurntSushi/ripgrep">ripgrep</a></p>
<br>
<img src="./imgs/screen2.png" width="600px">
